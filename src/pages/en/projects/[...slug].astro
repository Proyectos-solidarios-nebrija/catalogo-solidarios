---
import { getCollection } from 'astro:content'
import BlogLayout from '@layouts/BlogLayout.astro'

export async function getStaticPaths() {
  const pages = await getCollection('proyectos', ({ id }) =>
    id.startsWith('es/')
  )

  const paths = pages.map((page) => {
    const [, ...slug] = page.slug.split('/')
    return { params: { slug: slug.join('/') || undefined }, props: page }
  })
  return paths
}

const page = Astro.props
const { title, imgPath, yearStart, yearEnd } = page.data
const { Content } = await page.render()
---

<BlogLayout title={title} imgPath={imgPath} year={`${yearStart}/${yearEnd}`}>
  <Content />
</BlogLayout>
